{
  "name": "learn2play-testing",
  "version": "1.0.0",
  "description": "Testing infrastructure for Learn2Play platform",
  "type": "module",
  "scripts": {
    "start:test-env": "docker-compose -f docker-compose.test.yml up -d",
    "stop:test-env": "docker-compose -f docker-compose.test.yml down",
    "reset:test-env": "docker-compose -f docker-compose.test.yml down -v && docker-compose -f docker-compose.test.yml up -d",
    "test:unit:all": "cd ../frontend && npm run test:unit && cd ../backend && npm run test:unit",
    "test:integration:all": "cd ../backend && npm run test:integration",
    "test:e2e:all": "cd ../frontend && npm run test:e2e",
    "test:all": "npm run test:unit:all && npm run test:integration:all && npm run test:e2e:all",
    "test:coverage": "cd ../frontend && npm run test:coverage && cd ../backend && npm run test:coverage",
    "test:ui": "cd ../frontend && npm run test:e2e -- --headed",
    "validate-test-env": "node validate-test-env.js",
    "fix-test-environment": "node fix-test-environment.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "concurrently": "^8.2.2"
  }
} 